spring:
  profiles:
    active: local
  application:
    name: financial-ledge-backend

server:
  port: 8080

jwt:
  secret: your-256-bit-secret-key-for-jwt-token-generation-minimum-32-characters-please-change-in-production
  expiration: 86400000

logging:
  level:
    root: INFO

